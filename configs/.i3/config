
set $mod Mod4

#font pango:CodingFontTobi 12
font pango:ProggyVectorDotted 3

# --misc shortcuts--

# print utility
bindsym Print --release exec --no-startup-id scrot -fs ~/Screenshots/%F-%H%M%S.png

# print to clipboard with xclip
bindsym Shift+Print --release exec scrot -s -e 'xclip -selection clipboard -t image/png -i $f' 

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'exit i3?' -B 'yes' 'i3-msg exit'"

# switch keyboard locale
bindsym Mod1+ctrl+space exec setxkbmap -layout br
bindsym ctrl+Shift+space exec setxkbmap -layout us

# xkill
bindsym Ctrl+$mod+x --release exec --no-startup-id xkill

# sticky window
bindsym $mod+Shift+s sticky toggle

# --volume adjust with pactl--
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# --lock screen--

bindsym XF86ScreenSaver exec --no-startup-id i3lock

# --brightness control--
bindsym XF86MonBrightnessDown exec brightnessctl set 20%-
bindsym XF86MonBrightnessUp exec brightnessctl set +20%


# 	======= AUTO START APPLICATIONS ======

exec --no-startup-id dex --autostart --environment i3

# --adsakdad---
exec --no-startup-id autorandr -c 

# --lock--
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock

# --networkmanager applet--
exec --no-startup-id nm-applet

# --poweralert--
exec poweralertd

# --startup script--
exec --no-startup-id $HOME/startup.sh

# --path variables--

# qt themes
exec --no-startup-id export QT_QPA_PPLATFORMTHEME=qt6ct
exec --no-startup-id export QT_QPA_PPLATFORMTHEME=qt5ct


# 	======== WINDOWS ========

# --border--
default_border pixel
default_floating_border normal
hide_edge_borders none
gaps inner 0
gaps outer 0

bindsym $mod+u border none
bindsym $mod+y border pixel 1
bindsym $mod+n border normal


# --window management--

# self explanatory
focus_follows_mouse yes

# start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec urxvt

# start a floating terminal
bindsym $mod+Shift+Return exec "urxvt -e sh -c 'i3-msg floating toggle border normal; clear; bash'"

# kill focused window
bindsym $mod+Shift+q kill

# toggle split horizontal vertical orientation
bindsym $mod+q split toggle

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# mouse+$mod to move floating windows
floating_modifier $mod

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child
# start dmenu (a program launcher)
bindsym $mod+d exec --no-startup-id dmenu_run -fn "ProggyVectorDotted-10"

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# scratchpad
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"


# 	======== WORKSPACES ========

# workspace names
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# movement
bindsym $mod+Ctrl+l workspace next
bindsym $mod+Ctrl+h workspace prev
bindsym $mod+b workspace back_and_forth

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10
bindsym $mod+Shift+b move container to workspace back_and_forth

# move focused monitor
bindsym ctrl+shift+l move workspace to output right 
bindsym ctrl+shift+h move workspace to output left
bindsym ctrl+shift+j move workspace to output down 
bindsym ctrl+shift+k move workspace to output up 

# --application specific workspaces--

# workspace 1
# workspace 2
#assign [class="firefox"] $ws2
# workspace 3
# workspace 4
assign [class="discord"] $ws4
# workspace 5
assign [class="nemo"] $ws5
# workspace 6
# workspace 7
# workspace 8
# workspace 9
# workspace misc

# --floating/border modifiers--
for_window [class="Pavucontrol"] floating enable border normal resize set 500 500
for_window [title="Add interval"] floating enable border pixel

# focus on last opened window
for_window [urgent=latest] focus

#	======== BAR SETTINGS ========
bar {
        status_command i3status
	position bottom
	font pango:CodingFontTobi 12
}

# hide bar
bindsym $mod+m bar mode toggle
